{
  "name": "@ai-translator/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=../.env --env-file-if-exists=../supabase/functions/.env src/server.ts",
    "token": "node --env-file-if-exists=../.env dev/dev-token.ts",
    "compile": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@hono/node-server": "^2.1.1",
    "@types/node": "^24.5.2",
    "hono": "^4.13.8",
    "openai": "^7.18.0",
    "postgres": "^3.4.9",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
